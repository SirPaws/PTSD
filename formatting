

all the specifiers for the standard printf function are also supported
https://en.cppreference.com/w/c/io/fprintf

these are extra:

%S      String | pretty much identical to %s but for String

%b      Binary | for u32 for bigger or smaller prepend (hh, h, l, ll, j, z, t, L)
%CB     CallBack | passed in a callback formatter 
%Cbg    Color background
%Cfg    Color foreground
%Cc     Color clear | resets color 


pPrintf("%Cfg(255, 0, 255)hello world%Cc\n");
this will print the text with a red color


String str = {  .length = sizeof("Hello World!\n") - 1, "Hello World!\n" };
pPrintf("%S", str);
    outputs:
        Hello World!

pPrintf("%hhb", 10)
    outputs:
        1010



va_list VectorFormatter(String extra_args, va_list list);
pPrintf("%CB", VectorFormatter, (vec3){ 1, 2, 3 });


